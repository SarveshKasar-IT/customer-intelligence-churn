# Runtime dependencies for the API / Docker image (pinned to tested versions).
# scikit-learn MUST match the version the model was trained with (1.9.0),
# because a pickled model is only guaranteed to load on the same version.
scikit-learn==1.9.0
pandas==3.0.6
numpy==2.5.3
joblib==1.6.0
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
xgboost==3.4.1
